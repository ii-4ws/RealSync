/**
 * Media configuration for Landing-1 story sections.
 * Set src values to actual asset paths when ready.
 * While null, components render branded placeholders.
 */
export const mediaConfig = {
  deepfake: {
    realSrc: null as string | null,
    fakeSrc: null as string | null,
  },
  laptop: {
    src: null as string | null,
  },
};
